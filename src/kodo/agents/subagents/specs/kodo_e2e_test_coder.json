{
  "name": "kodo_e2e_test_coder",
  "notes": "SubAgentSpec for `e2e_test_coder` (stage 9 author, paired with e2e_test_code_critic).\n\n`e2e_test_coder` implements the product-level integration suite the accepted\nEnd-to-End Test Plan designs: the harness that assembles the whole system as a\nblack box, the local mock servers standing in for external dependencies, the\nconfiguration injection through the declared seams, and the behavioral\nassertions per scenario. It runs the suite via `toolchain_build` and iterates\nto a clean state before the critic sees it; a genuine system-behavior mismatch\nis surfaced to the guide as an escalation (a `reason` on its `return_result`)\nrather than papered over.",
  "input_schema": {
    "shape": "pipeline_input",
    "input_paths": "The End-to-End Test Plan (inventory + Mock Specifications + scenarios), the architecture (Part 3 seams), the Tech Stack, the requirements, the Narrative, the Design Plan, and all Functional Designs (consumed external interfaces + seams). Production code may be read only to learn the system's boundary (entry point, config), never to assert on internals."
  },
  "output_schema": {
    "shape": "author_output"
  },
  "produces": {
    "e2e_test_code": "paths"
  },
  "consumes": [
    {
      "role": "e2e_test_plan",
      "scope": "global"
    },
    {
      "role": "architecture",
      "scope": "global"
    },
    {
      "role": "tech_stack",
      "scope": "global"
    },
    {
      "role": "requirements",
      "scope": "global"
    },
    {
      "role": "narrative",
      "scope": "global"
    },
    {
      "role": "design_plan",
      "scope": "global"
    },
    {
      "role": "functional_design",
      "scope": "all"
    }
  ]
}
