"""Built-in default security ruleset — future iteration.

Will seed :mod:`._store` with sensible built-in rules once the rule engine
(:mod:`._rules`) exists. Not yet implemented; today's built-in behavior is
the mode logic in :mod:`._layer`.
"""

from __future__ import annotations

__all__: list[str] = []
