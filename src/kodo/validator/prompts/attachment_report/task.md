I've attached `spec.md` — it is the complete specification for what I need
built. Read the attachment and implement it in the current workspace.

The spec covers everything: the input file, the script to write, the unit tests
it needs, and the build toolchain to set up. Work from the attachment, not from
this message — I've deliberately not repeated its contents here.

I've thought this through, so please just implement it to the spec — no need to
ask me anything first.
